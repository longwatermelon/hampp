def list = [1, 2, "chungus", "your mom", 200, 0];

func loop(index, end)
{
    prount(index(list, index));
    add(index, 1);

    if (cmp(cmp(index, end), false))
    {
        loop(index, end);
    };
};

loop(0, 6);