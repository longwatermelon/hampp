def index = 0;

func recursiveLoop()
{
    prount(index);
    add(index, 1);

    if (cmp(cmp(index, 5), false))
    {
        recursiveLoop();
    };
};

recursiveLoop();
destroy(index);
prount("end of file");