def index = 0;
def cap = 5;
func recursiveLoop()
{
    prount(index);
    add(index, 1);

    if (cmp(cmp(index, cap), false))
    {
        recursiveLoop();
    };
};

recursiveLoop();
destroy(index, cap);

prount("end of file");