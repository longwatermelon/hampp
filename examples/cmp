def variable = "test";
def condition = strcmp(variable, "test");

if (condition)
{
    prount("variable and test are equal");
};

if (strcmp(variable, "test"))
{
    prount("variable and test are equal");
};

if (strcmp(variable, "this is not test"))
{
    prount("variable and this is not test are equal");
};